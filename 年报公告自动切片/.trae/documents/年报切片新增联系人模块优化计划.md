# 年报切片程序优化计划 - 新增"联系人"模块

## 一、需求确认

### 1.1 现有程序结构
- **主程序**: `年报自动处理系统/processors/ldrjs_processor.py` - 领导人介绍处理器
- **测试程序**: `领导人介绍年报.py` - 独立测试脚本

### 1.2 新增需求
| 项目 | 内容 |
|------|------|
| 模块名称 | 联系人 |
| 披露位置 | 5-8页（常见），搜索范围：3-15页 |
| 适用范围 | 仅上交所(shs)和深交所(szs)，北交所(bjs)保持原逻辑 |
| 开始关键词 | 匹配规则：`[一二三四][、.．]?\s*联系人和联系方式`<br>搜索关键词：`联系人` |
| 结束关键词 | 匹配规则：`[二三四五][、.．]?\s*(?:基本情况简介|信息披露及备置地点)`<br>搜索关键词：`["基本情况", "备置地点"]` |
| 输出方式 | 与领导人介绍合并输出到同一PDF，但**不包含中间内容** |

### 1.3 处理逻辑

```
北交所(bjs): 只处理领导人介绍（保持原逻辑）

上交所(shs)/深交所(szs): 
  情况1：都没找到 → 跳过处理
  情况2：只找到联系人 → 只输出联系人切片
  情况3：只找到领导人介绍 → 只输出领导人介绍切片
  情况4：都找到了 → 输出联系人切片 + 领导人介绍切片（不含中间内容）
```

---

## 二、联系人模块详细处理规则（重要）

### 2.1 联系人模块特点
- **内容很短**：通常在1页，最多2页
- **不存在中间完整页**：不需要考虑开始页和结束页之间的完整页

### 2.2 联系人模块处理逻辑（简化版）

| 情况 | 处理规则 | 输出内容 |
|------|----------|----------|
| **找到开始和结束，同一页** | 裁剪该页，保留开始到结束之间的内容 | 1页裁剪 |
| **找到开始和结束，两页** | 开始页裁剪(保留之后内容) + 结束页裁剪(保留之前内容) | 2页裁剪 |
| **只找到开始关键词** | 当前页裁剪(保留之后内容) + 下一页(完整页) | 2页 |
| **只找到结束关键词** | 上一页(完整页) + 当前页裁剪(保留之前内容) | 2页 |
| **都没找到** | 跳过处理 | None |

### 2.3 处理规则详解

#### 情况1：找到开始和结束关键词（同一页）
```
输入：start_info.page_number == end_info.page_number

输出结构：
┌─────────────────────────────────────┐
│ 单页裁剪（保留开始关键词到结束关键词之间的内容）│
└─────────────────────────────────────┘
```

#### 情况2：找到开始和结束关键词（两页）
```
输入：start_info.page_number + 1 == end_info.page_number

输出结构：
┌─────────────────────────────────────┐
│ 开始页（裁剪：保留关键词之后内容）    │
├─────────────────────────────────────┤
│ 结束页（裁剪：保留关键词之前内容）    │
└─────────────────────────────────────┘
```

#### 情况3：只找到开始关键词
```
输入：只有 start_info

输出结构：
┌─────────────────────────────────────┐
│ 开始页（裁剪：保留关键词之后内容）    │
├─────────────────────────────────────┤
│ 下一页（完整页）                     │
└─────────────────────────────────────┘
```

#### 情况4：只找到结束关键词
```
输入：只有 end_info

输出结构：
┌─────────────────────────────────────┐
│ 上一页（完整页）                     │
├─────────────────────────────────────┤
│ 结束页（裁剪：保留关键词之前内容）    │
└─────────────────────────────────────┘
```

### 2.4 与领导人介绍处理规则对比

| 项目 | 联系人模块 | 领导人介绍模块 |
|------|-----------|---------------|
| 内容长度 | 1-2页 | 多页（可能10页以上） |
| 只找到开始 | 当前页裁剪 + 下一页完整 | 当前页裁剪 + 后4页完整 |
| 只找到结束 | 上一页完整 + 当前页裁剪 | 前5页完整 + 当前页裁剪 |
| 中间完整页 | 不存在 | 存在，需要处理 |
| 搜索范围 | 3-15页 | 15-50页、45-70页、65-130页 |

---

## 三、主程序优化方案

### 3.1 文件修改：`ldrjs_processor.py`

#### 3.1.1 新增联系人关键词查找方法
```python
def find_lxr_keywords(self, pdf_path: str) -> Dict:
    """查找联系人关键词（仅上交所和深交所）"""
    exchange_code = get_exchange_code(pdf_path)
    
    # 北交所不处理联系人
    if exchange_code == "bjs":
        return {'start': None, 'end': None}
    
    # 开始关键词匹配规则
    start_pattern = re.compile(
        r'[一二三四][、.．]?\s*联系人和联系方式',
        re.IGNORECASE | re.MULTILINE
    )
    
    # 结束关键词匹配规则
    end_pattern = re.compile(
        r'[二三四五][、.．]?\s*(?:基本情况简介|信息披露及备置地点)',
        re.IGNORECASE | re.MULTILINE
    )
    
    doc = fitz.open(pdf_path)
    total_pages = len(doc)
    
    start_info = None
    end_info = None
    
    # 搜索范围：3-15页
    search_ranges = [(3, 15)]
    
    # 搜索关键词
    start_keywords = ["联系人"]
    end_keywords = ["基本情况", "备置地点"]
    
    # ... 搜索逻辑（复用现有的搜索框架）
    
    doc.close()
    
    return {
        'start': start_info,
        'end': end_info
    }
```

#### 3.1.2 新增联系人单独处理方法（简化版）
```python
def _process_lxr_only(self, pdf_path: str, lxr_info: Dict) -> Optional[str]:
    """单独处理联系人切片
    
    处理规则（简化版，联系人通常只有1-2页）：
    - 同一页：裁剪该页
    - 两页：开始页裁剪 + 结束页裁剪
    - 只找到开始：当前页裁剪 + 下一页完整
    - 只找到结束：上一页完整 + 当前页裁剪
    """
    start_info = lxr_info.get('start')
    end_info = lxr_info.get('end')
    
    # 情况1：都没找到
    if not start_info and not end_info:
        return None
    
    # 情况2：只找到结束关键词
    if not start_info and end_info:
        return self._crop_lxr_end_only(pdf_path, end_info)
    
    # 情况3：只找到开始关键词
    if start_info and not end_info:
        return self._crop_lxr_start_only(pdf_path, start_info)
    
    # 情况4：都找到了
    if start_info and end_info:
        # 开始页大于结束页，按只找到结束处理
        if start_info['page_number'] > end_info['page_number']:
            return self._crop_lxr_end_only(pdf_path, end_info)
        
        # 同一页：裁剪该页
        if start_info['page_number'] == end_info['page_number']:
            return crop_same_page(pdf_path, start_info, end_info, self.output_dir)
        
        # 两页：开始页裁剪 + 结束页裁剪
        if start_info['page_number'] + 1 == end_info['page_number']:
            return self._crop_lxr_two_pages(pdf_path, start_info, end_info)
        
        # 超过两页：按只找到结束处理（异常情况，暂不处理）
        return self._crop_lxr_end_only(pdf_path, end_info)
    
    return None
```

#### 3.1.3 新增联系人只找到开始关键词处理方法
```python
def _crop_lxr_start_only(self, pdf_path: str, start_info: Dict) -> Optional[str]:
    """只找到开始关键词：当前页裁剪 + 下一页完整"""
    from PyPDF2 import PdfReader, PdfWriter
    
    reader = PdfReader(pdf_path)
    writer = PdfWriter()
    
    page_num = start_info['page_number']
    page = reader.pages[page_num - 1]
    
    # 裁剪开始页：保留开始关键词之后的内容
    page_width, page_height = start_info['page_dimensions']
    min_x, min_y, max_x, max_y = start_info['keyword_box']
    page_rotation = page.rotation
    
    pydf2_max_y = page_height - min_y
    
    cropped_page = page
    if page_rotation == 90:
        cropped_page.cropbox.lower_left = (min_x, 0)
        cropped_page.cropbox.upper_right = (page_height, page_width)
    else:
        cropped_page.cropbox.lower_left = (0, 0)
        cropped_page.cropbox.upper_right = (page_width, pydf2_max_y)
    
    writer.add_page(cropped_page)
    
    # 添加下一页（完整页）
    if page_num < len(reader.pages):
        writer.add_page(reader.pages[page_num])
    
    # 保存
    base_name = os.path.splitext(os.path.basename(pdf_path))[0]
    output_path = os.path.join(self.output_dir, f"{base_name}.pdf")
    
    if not os.path.exists(self.output_dir):
        os.makedirs(self.output_dir)
    
    with open(output_path, "wb") as output_file:
        writer.write(output_file)
    
    return output_path
```

#### 3.1.4 新增联系人只找到结束关键词处理方法
```python
def _crop_lxr_end_only(self, pdf_path: str, end_info: Dict) -> Optional[str]:
    """只找到结束关键词：上一页完整 + 当前页裁剪"""
    from PyPDF2 import PdfReader, PdfWriter
    
    reader = PdfReader(pdf_path)
    writer = PdfWriter()
    
    page_num = end_info['page_number']
    
    # 添加上一页（完整页）
    if page_num > 1:
        writer.add_page(reader.pages[page_num - 2])
    
    # 裁剪结束页：保留结束关键词之前的内容
    page = reader.pages[page_num - 1]
    page_width, page_height = end_info['page_dimensions']
    min_x, min_y, max_x, max_y = end_info['keyword_box']
    page_rotation = page.rotation
    
    if page_rotation == 90:
        pydf2_min_y = page_height - max_x
    else:
        pydf2_min_y = page_height - max_y
    
    cropped_page = page
    if page_rotation == 90:
        cropped_page.cropbox.lower_left = (0, 0)
        cropped_page.cropbox.upper_right = (max_x, page_width)
    else:
        cropped_page.cropbox.lower_left = (0, pydf2_min_y)
        cropped_page.cropbox.upper_right = (page_width, page_height)
    
    writer.add_page(cropped_page)
    
    # 保存
    base_name = os.path.splitext(os.path.basename(pdf_path))[0]
    output_path = os.path.join(self.output_dir, f"{base_name}.pdf")
    
    if not os.path.exists(self.output_dir):
        os.makedirs(self.output_dir)
    
    with open(output_path, "wb") as output_file:
        writer.write(output_file)
    
    return output_path
```

#### 3.1.5 新增联系人两页裁剪方法
```python
def _crop_lxr_two_pages(self, pdf_path: str, start_info: Dict, end_info: Dict) -> Optional[str]:
    """联系人两页裁剪：开始页裁剪 + 结束页裁剪"""
    from PyPDF2 import PdfReader, PdfWriter
    
    reader = PdfReader(pdf_path)
    writer = PdfWriter()
    
    # 处理开始页
    start_page = reader.pages[start_info['page_number'] - 1]
    page_width, page_height = start_info['page_dimensions']
    min_x, min_y, max_x, max_y = start_info['keyword_box']
    page_rotation = start_page.rotation
    
    pydf2_max_y = page_height - min_y
    
    cropped_start = start_page
    if page_rotation == 90:
        cropped_start.cropbox.lower_left = (min_x, 0)
        cropped_start.cropbox.upper_right = (page_height, page_width)
    else:
        cropped_start.cropbox.lower_left = (0, 0)
        cropped_start.cropbox.upper_right = (page_width, pydf2_max_y)
    
    writer.add_page(cropped_start)
    
    # 处理结束页
    end_page = reader.pages[end_info['page_number'] - 1]
    page_width, page_height = end_info['page_dimensions']
    min_x, min_y, max_x, max_y = end_info['keyword_box']
    page_rotation = end_page.rotation
    
    if page_rotation == 90:
        pydf2_min_y = page_height - max_x
    else:
        pydf2_min_y = page_height - max_y
    
    cropped_end = end_page
    if page_rotation == 90:
        cropped_end.cropbox.lower_left = (0, 0)
        cropped_end.cropbox.upper_right = (max_x, page_width)
    else:
        cropped_end.cropbox.lower_left = (0, pydf2_min_y)
        cropped_end.cropbox.upper_right = (page_width, page_height)
    
    writer.add_page(cropped_end)
    
    # 保存
    base_name = os.path.splitext(os.path.basename(pdf_path))[0]
    output_path = os.path.join(self.output_dir, f"{base_name}.pdf")
    
    if not os.path.exists(self.output_dir):
        os.makedirs(self.output_dir)
    
    with open(output_path, "wb") as output_file:
        writer.write(output_file)
    
    return output_path
```

#### 3.1.6 新增合并查找方法
```python
def find_all_keywords(self, pdf_path: str) -> Dict:
    """查找所有关键词（联系人+领导人介绍）"""
    exchange_code = get_exchange_code(pdf_path)
    
    if exchange_code == "bjs":
        return {
            'lxr': {'start': None, 'end': None},
            'ldrjs': self.find_keywords(pdf_path)
        }
    else:
        return {
            'lxr': self.find_lxr_keywords(pdf_path),
            'ldrjs': self.find_keywords(pdf_path)
        }
```

#### 3.1.7 修改 `process_pdf` 方法
```python
def process_pdf(self, pdf_path: str, keywords: Dict) -> Optional[str]:
    """处理PDF（联系人+领导人介绍合并输出）"""
    exchange_code = get_exchange_code(pdf_path)
    
    # 获取关键词信息
    if 'lxr' in keywords:
        lxr_info = keywords['lxr']
        ldrjs_info = keywords['ldrjs']
    else:
        lxr_info = {'start': None, 'end': None}
        ldrjs_info = keywords
    
    # 北交所：只处理领导人介绍
    if exchange_code == "bjs":
        return self._process_ldrjs_only(pdf_path, ldrjs_info)
    
    # 上交所/深交所：合并处理
    return self._process_combined(pdf_path, lxr_info, ldrjs_info)
```

#### 3.1.8 新增合并处理方法
```python
def _process_combined(self, pdf_path: str, lxr_info: Dict, ldrjs_info: Dict) -> Optional[str]:
    """合并处理联系人和领导人介绍"""
    lxr_start = lxr_info.get('start')
    ldrjs_start = ldrjs_info.get('start')
    
    # 情况1：都没找到
    if not lxr_start and not ldrjs_start:
        return None
    
    # 情况2：只找到联系人
    if lxr_start and not ldrjs_start:
        return self._process_lxr_only(pdf_path, lxr_info)
    
    # 情况3：只找到领导人介绍
    if not lxr_start and ldrjs_start:
        return self._process_ldrjs_only(pdf_path, ldrjs_info)
    
    # 情况4：都找到了，合并输出
    return self._merge_slices(pdf_path, lxr_info, ldrjs_info)
```

#### 3.1.9 新增合并切片方法
```python
def _merge_slices(self, pdf_path: str, lxr_info: Dict, ldrjs_info: Dict) -> Optional[str]:
    """合并联系人和领导人介绍切片"""
    from PyPDF2 import PdfReader, PdfWriter
    
    reader = PdfReader(pdf_path)
    writer = PdfWriter()
    
    # 添加联系人切片
    self._add_lxr_slice_to_writer(reader, writer, lxr_info)
    
    # 添加领导人介绍切片
    self._add_ldrjs_slice_to_writer(reader, writer, ldrjs_info)
    
    # 保存
    base_name = os.path.splitext(os.path.basename(pdf_path))[0]
    output_path = os.path.join(self.output_dir, f"{base_name}.pdf")
    
    if not os.path.exists(self.output_dir):
        os.makedirs(self.output_dir)
    
    with open(output_path, "wb") as output_file:
        writer.write(output_file)
    
    return output_path
```

---

## 四、测试程序优化方案

### 4.1 文件修改：`领导人介绍年报.py`

#### 4.1.1 新增联系人关键词查找函数
```python
def find_lxr_keywords(pdf_path):
    """查找联系人关键词"""
    # ... 实现逻辑
```

#### 4.1.2 新增联系人单独处理函数（简化版）
```python
def process_lxr_only(pdf_path, lxr_info, output_dir):
    """单独处理联系人切片（简化版）"""
    start_info = lxr_info.get('start')
    end_info = lxr_info.get('end')
    
    if not start_info and not end_info:
        return None
    
    if not start_info and end_info:
        return crop_lxr_end_only(pdf_path, end_info, output_dir)
    
    if start_info and not end_info:
        return crop_lxr_start_only(pdf_path, start_info, output_dir)
    
    if start_info and end_info:
        if start_info['page_number'] > end_info['page_number']:
            return crop_lxr_end_only(pdf_path, end_info, output_dir)
        
        if start_info['page_number'] == end_info['page_number']:
            return crop_same_page(pdf_path, start_info, end_info, output_dir)
        
        if start_info['page_number'] + 1 == end_info['page_number']:
            return crop_lxr_two_pages(pdf_path, start_info, end_info, output_dir)
        
        return crop_lxr_end_only(pdf_path, end_info, output_dir)
    
    return None
```

#### 4.1.3 新增联系人裁剪函数
```python
def crop_lxr_start_only(pdf_path, start_info, output_dir):
    """只找到开始：当前页裁剪 + 下一页完整"""
    # ... 实现逻辑

def crop_lxr_end_only(pdf_path, end_info, output_dir):
    """只找到结束：上一页完整 + 当前页裁剪"""
    # ... 实现逻辑

def crop_lxr_two_pages(pdf_path, start_info, end_info, output_dir):
    """两页裁剪：开始页裁剪 + 结束页裁剪"""
    # ... 实现逻辑
```

---

## 五、实施步骤

### 步骤1：修改主程序 `ldrjs_processor.py`
1. 新增 `find_lxr_keywords` 方法
2. 新增 `find_all_keywords` 方法
3. 新增 `_process_lxr_only` 方法（简化版）
4. 新增 `_crop_lxr_start_only` 方法
5. 新增 `_crop_lxr_end_only` 方法
6. 新增 `_crop_lxr_two_pages` 方法
7. 修改 `process_pdf` 方法
8. 新增 `_process_combined` 方法
9. 新增 `_merge_slices` 方法

### 步骤2：修改测试程序 `领导人介绍年报.py`
1. 新增 `find_lxr_keywords` 函数
2. 新增 `find_all_keywords` 函数
3. 新增 `process_lxr_only` 函数（简化版）
4. 新增 `crop_lxr_start_only` 函数
5. 新增 `crop_lxr_end_only` 函数
6. 新增 `crop_lxr_two_pages` 函数
7. 修改 `process_pdf` 函数
8. 新增 `process_combined` 函数
9. 新增 `merge_slices` 函数

### 步骤3：测试验证
1. 使用上交所/深交所PDF测试合并输出
2. 使用北交所PDF验证原逻辑不变
3. 测试边界情况

---

## 六、关键代码变更点

### 6.1 联系人处理规则（简化版）
| 情况 | 处理方式 |
|------|----------|
| 同一页 | `crop_same_page` |
| 两页 | `_crop_lxr_two_pages` |
| 只找到开始 | `_crop_lxr_start_only` |
| 只找到结束 | `_crop_lxr_end_only` |

### 6.2 关键词匹配规则
```python
# 联系人开始
lxr_start_pattern = re.compile(
    r'[一二三四][、.．]?\s*联系人和联系方式',
    re.IGNORECASE | re.MULTILINE
)

# 联系人结束
lxr_end_pattern = re.compile(
    r'[二三四五][、.．]?\s*(?:基本情况简介|信息披露及备置地点)',
    re.IGNORECASE | re.MULTILINE
)
```

---

## 七、风险与注意事项

1. **关键词匹配准确性**：需要用实际PDF验证正则表达式
2. **页面旋转处理**：联系人搜索也需要处理90度旋转页面
3. **向后兼容**：北交所逻辑完全保持不变
4. **边界检查**：处理只找到开始/结束关键词时，需要检查是否有上一页/下一页
5. **异常情况**：如果联系人超过2页，按只找到结束关键词处理
