# 优化计划：净资产收益率/每股收益模块分开输出

## 需求概述

将净资产收益率\[及和]每股收益模块的处理结果从原合并输出改为分开输出。

## 当前逻辑

* `process_pdf()` 函数在处理完主要指标后，会将追加内容（净资产收益率/每股收益）合并到主PDF后面

* 最终只输出一个PDF文件

## 目标输出结构

```
主要指标/
├── {base_name}.pdf              # 原模块处理后的文件
└── 净资产收益率和每股收益/
    └── {base_name}_mgsy.pdf    # 净资产收益率/每股收益模块文件
```

## 实现步骤

### 步骤1：新增函数 `save_append_content_pdf()`

创建一个新函数，专门用于将净资产收益率/每股收益模块单独保存为PDF文件：

* 输入参数：源PDF路径、append\_start\_info、append\_end\_info、输出目录

* 输出路径：`{output_dir}/净资产收益率和每股收益/{base_name}_mgsy.pdf`

* 自动创建子目录

* 调用现有的 `extract_append_pages()` 提取页面

### 步骤2：修改 `process_pdf()` 函数

移除合并追加内容的逻辑：

* 删除将 append\_pages 添加到主 writer 的代码

* 改为调用新的 `save_append_content_pdf()` 函数单独保存

* 主PDF输出路径保持不变：`{output_dir}/{base_name}.pdf`

### 步骤3：修改 `append_content_to_pdf()` 函数的调用位置

该函数目前用于合并输出，需要改为调用新的单独保存函数。

### 步骤4：测试验证

确保修改后的代码能正确输出两个独立的PDF文件。

## 具体代码修改位置

### 修改点1：新增 `save_append_content_pdf()` 函数（约在第569行后）

```python
def save_append_content_pdf(pdf_path, append_start_info, append_end_info, output_dir):
    """
    将净资产收益率/每股收益模块单独保存为PDF
    
    Args:
        pdf_path: 源PDF路径
        append_start_info: 开始关键词信息
        append_end_info: 结束关键词信息
        output_dir: 主输出目录
    
    Returns:
        str: 输出文件路径，如果无内容则返回None
    """
```

### 修改点2：修改 `process_pdf()` 函数中的追加内容处理逻辑

* 第746-750行：删除合并逻辑，改为调用新函数

* 第621-625行：同上

* 第595-596行、602-603行、610-611行：修改 `append_content_to_pdf` 的调用

## 文件修改清单

* `e:\Project\年报公告自动切片\主要指标年报.py`：仅修改此测试文件

